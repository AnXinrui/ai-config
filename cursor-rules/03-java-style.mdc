---
description: Java 编码规范：阿里巴巴规范、POJO、日志、SQL、格式
globs: **/*.java
alwaysApply: false
---

# Java 编码规范

遵循阿里巴巴开发规范与命名规范。

## POJO

- 属性使用包装类型（Integer、Long、Boolean），禁止基本类型
- 推荐 `@Data`，必须实现 toString

## 线程池

禁止 `Executors`，使用 `new ThreadPoolExecutor(...)`

## 日志

禁止字符串拼接，使用占位符：

```java
log.info("用户id：{}", userId);
```

## 异常处理

禁止吞异常，catch 块必须记录日志：

```java
catch (Exception e) {
    log.error("查询失败", e);
}
```

## SQL

- 关键字大写（SELECT、FROM、WHERE）
- 禁止 `SELECT *`，必须明确字段

## 格式

- if/for/while 必须大括号
- 运算符两边空格
- Stream 链式调用换行对齐
- 禁止魔法值

## 新建 Java 类

类注释格式：

```java
/**
 * @description xxx
 * @author xinrui.an
 * @date yyyy/MM/dd
 */
```

## 原则

优先复用现有代码，避免重复实现。
